import * as THREE from 'three';

const scene = new THREE.scene //tempat naro aset 3d
const kamera = new THREE.PerspectiveCamera()

const render = new THREE.WebGL3DRenderTarget()
const Adam = () => {
  return(
    <div id="">

    </div>
  )
}

export default Adam